# ==================== SERVER CONFIG ====================
server.port=8080
server.servlet.context-path=/api

# ==================== DATABASE CONFIG ====================
spring.datasource.url=jdbc:mysql://localhost:3306/rikkei_bank?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=datloc78
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==================== JPA / HIBERNATE ====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ==================== JWT CONFIG ====================
jwt.secret=RikkeiBankSuperSecretKey2026MustBeAtLeast256BitsLongForHS256
jwt.access-expiration=300000
jwt.refresh-expiration=86400000

# ==================== FILE UPLOAD (eKYC) ====================
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

# ==================== LOGGING ====================
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG